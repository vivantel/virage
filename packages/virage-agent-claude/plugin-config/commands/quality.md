---
description: Run virage pipeline self-assessment and quality metrics
---

Run `virage quality $ARGUMENTS` via Bash to assess the RAG pipeline health.

Report the overall score, component breakdown (chunking, metadata, embedding, retrieval), and any must-pass gate failures. Suggest concrete fixes for any failing metrics.
