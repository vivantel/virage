Load and follow the Virage architect skill to design the requested system change or write an ADR.
$ARGUMENTS
Call `mcp__virage__read_skill` with `name: "architect"` to load the full architecture workflow and follow it precisely.
