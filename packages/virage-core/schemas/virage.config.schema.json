{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "https://github.com/vivantel/virage/schemas/virage.config.schema.json",
  "title": "Virage Pipeline Config",
  "description": "Declarative JSON configuration for @vivantel/virage-core",
  "type": "object",
  "required": ["chunking", "embedder", "vectorStore"],
  "additionalProperties": false,
  "properties": {
    "$schema": {
      "type": "string"
    },
    "chunking": {
      "type": "object",
      "required": ["chunkers"],
      "additionalProperties": false,
      "description": "Chunking configuration: global file exclusions and per-chunker rules.",
      "properties": {
        "exclude": {
          "type": "array",
          "items": { "type": "string" },
          "description": "Glob patterns for files to exclude globally from all chunkers (e.g. '**/vendor/**', '**/*.min.js')."
        },
        "chunkers": {
          "type": "array",
          "minItems": 1,
          "items": { "$ref": "#/$defs/chunkerItem" }
        }
      }
    },
    "chunkers": {
      "$comment": "DEPRECATED: use chunking.chunkers instead. Kept here so old configs pass schema validation before the runtime normalizer runs.",
      "type": "array",
      "minItems": 1,
      "items": { "$ref": "#/$defs/chunkerItem" }
    },
    "embedder": {
      "type": "object",
      "required": ["package"],
      "additionalProperties": false,
      "properties": {
        "package": {
          "type": "string",
          "description": "npm package that exports createEmbedder(config). E.g. '@vivantel/virage-embedder-openai'"
        },
        "config": {
          "type": "object",
          "description": "Config passed to the package's createEmbedder() factory. Supports ${ENV_VAR} expansion.",
          "additionalProperties": true
        }
      }
    },
    "vectorStore": {
      "type": "object",
      "required": ["package"],
      "additionalProperties": false,
      "properties": {
        "package": {
          "type": "string",
          "description": "npm package that exports createVectorStore(config). E.g. '@vivantel/virage-store-postgres'"
        },
        "config": {
          "type": "object",
          "description": "Config passed to the package's createVectorStore() factory. Supports ${ENV_VAR} expansion.",
          "additionalProperties": true
        }
      }
    },
    "agents": {
      "type": "array",
      "items": { "type": "string" },
      "description": "Coding agent plugin names selected during init (e.g. [\"claude-code\", \"copilot\"])."
    },
    "search": {
      "type": "object",
      "additionalProperties": false,
      "description": "Optional retrieval configuration: hybrid search and re-ranking.",
      "properties": {
        "hybrid": {
          "type": "boolean",
          "description": "Enable BM25 + vector hybrid search with Reciprocal Rank Fusion. Default: false."
        },
        "hybridAlpha": {
          "type": "number",
          "minimum": 0,
          "maximum": 1,
          "description": "Blend weight for hybrid search: 0 = pure BM25, 1 = pure vector. Default: 0.6."
        },
        "reranker": {
          "type": "object",
          "required": ["package"],
          "additionalProperties": false,
          "description": "Optional post-retrieval re-ranker plugin.",
          "properties": {
            "package": {
              "type": "string",
              "description": "npm package that exports createReranker(config). E.g. '@vivantel/virage-reranker-cross-encoder'"
            },
            "config": {
              "type": "object",
              "additionalProperties": true,
              "description": "Config passed to createReranker(). Supports ${ENV_VAR} expansion."
            }
          }
        }
      }
    },
    "pluginVersions": {
      "type": "object",
      "description": "Installed plugin package versions, recorded by virage init/update.",
      "additionalProperties": {
        "type": "string"
      }
    },
    "options": {
      "type": "object",
      "additionalProperties": false,
      "properties": {
        "force": { "type": "boolean" },
        "skipUpload": { "type": "boolean" },
        "dryRun": { "type": "boolean" },
        "rateLimitMs": { "type": "number" },
        "batchSize": { "type": "number" },
        "maxBatchChars": { "type": "number" },
        "concurrency": { "type": "number" },
        "chunkConcurrency": {
          "type": "number",
          "description": "Number of files to chunk concurrently. Defaults to the number of logical CPU cores."
        },
        "minIngestionBatchSize": {
          "type": "integer",
          "minimum": 1,
          "description": "Deprecated. Use minUploadingBatchSize instead."
        },
        "minEmbeddingBatchSize": {
          "type": "integer",
          "minimum": 1,
          "description": "Minimum pending-chunk queue size before triggering an embedding run. Default: 10."
        },
        "minUploadingBatchSize": {
          "type": "integer",
          "minimum": 1,
          "description": "Minimum embedded-chunk queue size before triggering an upload batch. Default: 20."
        },
        "noBanner": {
          "type": "boolean",
          "description": "Suppress the startup banner. Can also be set via VIRAGE_NO_BANNER=1 environment variable."
        },
        "telemetry": { "type": "boolean" },
        "notifications": {
          "type": "object",
          "additionalProperties": false,
          "properties": {
            "webhookUrl": { "type": "string" }
          }
        }
      }
    },
    "source": {
      "type": "object",
      "required": ["package"],
      "additionalProperties": false,
      "description": "Optional custom source repository plugin.",
      "properties": {
        "package": { "type": "string" },
        "config": { "type": "object", "additionalProperties": true }
      }
    }
  },
  "$defs": {
    "chunkerItem": {
      "type": "object",
      "required": ["patterns", "strategy"],
      "additionalProperties": false,
      "properties": {
        "name": {
          "type": "string",
          "description": "Optional name for this chunker. Auto-derived from strategy and pattern if omitted."
        },
        "patterns": {
          "type": "array",
          "minItems": 1,
          "items": { "type": "string" },
          "description": "Glob patterns for files this chunker handles."
        },
        "ignorePatterns": {
          "type": "array",
          "items": { "type": "string" },
          "description": "Glob patterns for files this chunker should skip (per-chunker excludes)."
        },
        "strategy": {
          "type": "string",
          "enum": ["markdownHeaders", "token", "wholeFile", "semantic", "codeChunkAst"],
          "description": "Built-in strategy name."
        },
        "strategyOptions": {
          "type": "object",
          "description": "Optional options passed to the strategy factory.",
          "properties": {
            "maxTokens": { "type": "number" },
            "overlap": { "type": "number" }
          },
          "additionalProperties": true
        }
      }
    }
  }
}
