# @vivantel/virage-git-isomorphic

Pure-JS git source repository for Virage using isomorphic-git — no subprocess spawning required. Used as a drop-in git backend for environments where spawning `git` is unavailable (e.g., WASM, sandboxed CI).
