# Skill: Onboarding (summary)

Use when: starting a new Claude Code session in a Virage project for the first time, re-orienting after a gap, running `virage init` or `virage update` in a new repo.

**Full skill cost:** ~525 tokens. Load with `read_skill('onboarding')` for the full self-configuration workflow.

## Key outputs
- All skills read and active in context
- MCP server registered and hooks installed
- Summary of environment state
