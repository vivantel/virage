name: Release

on:
  push:
    branches:
      - master

permissions:
  contents: write
  pull-requests: write
  issues: write
  packages: write
  pages: write
  id-token: write

jobs:
  release-please:
    runs-on: ubuntu-latest
    outputs:
      core_released: ${{ steps.release.outputs['packages/virage-core--release_created'] }}
      strategies_released: ${{ steps.release.outputs['packages/virage-strategies--release_created'] }}
      openai_released: ${{ steps.release.outputs['packages/virage-embedder-openai--release_created'] }}
      transformers_released: ${{ steps.release.outputs['packages/virage-embedder-transformers--release_created'] }}
      fastembed_released: ${{ steps.release.outputs['packages/virage-embedder-fastembed--release_created'] }}
      postgres_released: ${{ steps.release.outputs['packages/virage-store-postgres--release_created'] }}
      qdrant_released: ${{ steps.release.outputs['packages/virage-store-qdrant--release_created'] }}
      lancedb_released: ${{ steps.release.outputs['packages/virage-store-lancedb--release_created'] }}
      chromadb_released: ${{ steps.release.outputs['packages/virage-store-chromadb--release_created'] }}
      cli_released: ${{ steps.release.outputs['packages/virage-cli--release_created'] }}
      dashboard_released: ${{ steps.release.outputs['packages/virage-dashboard--release_created'] }}
    steps:
      - name: Run Release Please
        id: release
        uses: googleapis/release-please-action@v5
        with:
          token: ${{ secrets.RELEASE_TOKEN }}
          config-file: .github/config/release-please.json
          manifest-file: .release-please-manifest.json


  publish-core:
    name: Publish @vivantel/virage-core
    needs: release-please
    if: ${{ needs.release-please.outputs.core_released == 'true' }}
    runs-on: ubuntu-latest
    permissions:
      contents: read
      id-token: write
    steps:
      - uses: actions/checkout@v6
      - uses: actions/setup-node@v6
        with:
          node-version: '20.x'
          registry-url: 'https://registry.npmjs.org'
          cache: "npm"
      - run: npm ci
      - run: npm test --workspace packages/virage-core
      - run: npm run build --workspace packages/virage-core
      - name: Publish
        run: npm publish --provenance --access public
        working-directory: packages/virage-core
        env:
          NODE_AUTH_TOKEN: ${{ secrets.NPM_TOKEN }}

  publish-strategies:
    name: Publish @vivantel/virage-strategies
    needs: release-please
    if: ${{ needs.release-please.outputs.strategies_released == 'true' }}
    runs-on: ubuntu-latest
    permissions:
      contents: read
      id-token: write
    steps:
      - uses: actions/checkout@v6
      - uses: actions/setup-node@v6
        with:
          node-version: '20.x'
          registry-url: 'https://registry.npmjs.org'
          cache: "npm"
      - run: npm ci
      - run: npm run build --workspace packages/virage-core
      - run: npm run build --workspace packages/virage-strategies
      - name: Publish
        run: npm publish --provenance --access public
        working-directory: packages/virage-strategies
        env:
          NODE_AUTH_TOKEN: ${{ secrets.NPM_TOKEN }}

  publish-embedder-openai:
    name: Publish @vivantel/virage-embedder-openai
    needs: release-please
    if: ${{ needs.release-please.outputs.openai_released == 'true' }}
    runs-on: ubuntu-latest
    permissions:
      contents: read
      id-token: write
    steps:
      - uses: actions/checkout@v6
      - uses: actions/setup-node@v6
        with:
          node-version: '20.x'
          registry-url: 'https://registry.npmjs.org'
          cache: "npm"
      - run: npm ci
      - run: npm run build --workspace packages/virage-core
      - run: npm test --workspace packages/virage-embedder-openai
      - run: npm run build --workspace packages/virage-embedder-openai
      - name: Publish
        run: npm publish --provenance --access public
        working-directory: packages/virage-embedder-openai
        env:
          NODE_AUTH_TOKEN: ${{ secrets.NPM_TOKEN }}

  publish-embedder-transformers:
    name: Publish @vivantel/virage-embedder-transformers
    needs: release-please
    if: ${{ needs.release-please.outputs.transformers_released == 'true' }}
    runs-on: ubuntu-latest
    permissions:
      contents: read
      id-token: write
    steps:
      - uses: actions/checkout@v6
      - uses: actions/setup-node@v6
        with:
          node-version: '20.x'
          registry-url: 'https://registry.npmjs.org'
          cache: "npm"
      - run: npm ci --ignore-scripts
      - run: npm run build --workspace packages/virage-core
      - run: npm test --workspace packages/virage-embedder-transformers
      - run: npm run build --workspace packages/virage-embedder-transformers
      - name: Publish
        run: npm publish --provenance --access public
        working-directory: packages/virage-embedder-transformers
        env:
          NODE_AUTH_TOKEN: ${{ secrets.NPM_TOKEN }}

  publish-embedder-fastembed:
    name: Publish @vivantel/virage-embedder-fastembed
    needs: release-please
    if: ${{ needs.release-please.outputs.fastembed_released == 'true' }}
    runs-on: ubuntu-latest
    permissions:
      contents: read
      id-token: write
    steps:
      - uses: actions/checkout@v6
      - uses: actions/setup-node@v6
        with:
          node-version: '20.x'
          registry-url: 'https://registry.npmjs.org'
          cache: "npm"
      - run: npm ci --ignore-scripts
      - run: npm run build --workspace packages/virage-core
      - run: npm test --workspace packages/virage-embedder-fastembed
      - run: npm run build --workspace packages/virage-embedder-fastembed
      - name: Publish
        run: npm publish --provenance --access public
        working-directory: packages/virage-embedder-fastembed
        env:
          NODE_AUTH_TOKEN: ${{ secrets.NPM_TOKEN }}

  publish-store-postgres:
    name: Publish @vivantel/virage-store-postgres
    needs: release-please
    if: ${{ needs.release-please.outputs.postgres_released == 'true' }}
    runs-on: ubuntu-latest
    permissions:
      contents: read
      id-token: write
    steps:
      - uses: actions/checkout@v6
      - uses: actions/setup-node@v6
        with:
          node-version: '20.x'
          registry-url: 'https://registry.npmjs.org'
          cache: "npm"
      - run: npm ci --ignore-scripts
      - run: npm run build --workspace packages/virage-core
      - run: npm test --workspace packages/virage-store-postgres
      - run: npm run build --workspace packages/virage-store-postgres
      - name: Publish
        run: npm publish --provenance --access public
        working-directory: packages/virage-store-postgres
        env:
          NODE_AUTH_TOKEN: ${{ secrets.NPM_TOKEN }}

  publish-store-qdrant:
    name: Publish @vivantel/virage-store-qdrant
    needs: release-please
    if: ${{ needs.release-please.outputs.qdrant_released == 'true' }}
    runs-on: ubuntu-latest
    permissions:
      contents: read
      id-token: write
    steps:
      - uses: actions/checkout@v6
      - uses: actions/setup-node@v6
        with:
          node-version: '20.x'
          registry-url: 'https://registry.npmjs.org'
          cache: "npm"
      - run: npm ci
      - run: npm run build --workspace packages/virage-core
      - run: npm test --workspace packages/virage-store-qdrant
      - run: npm run build --workspace packages/virage-store-qdrant
      - name: Publish
        run: npm publish --provenance --access public
        working-directory: packages/virage-store-qdrant
        env:
          NODE_AUTH_TOKEN: ${{ secrets.NPM_TOKEN }}

  publish-store-lancedb:
    name: Publish @vivantel/virage-store-lancedb
    needs: release-please
    if: ${{ needs.release-please.outputs.lancedb_released == 'true' }}
    runs-on: ubuntu-latest
    permissions:
      contents: read
      id-token: write
    steps:
      - uses: actions/checkout@v6
      - uses: actions/setup-node@v6
        with:
          node-version: '20.x'
          registry-url: 'https://registry.npmjs.org'
          cache: "npm"
      - run: npm ci --ignore-scripts
      - run: npm run build --workspace packages/virage-core
      - run: npm test --workspace packages/virage-store-lancedb
      - run: npm run build --workspace packages/virage-store-lancedb
      - name: Publish
        run: npm publish --provenance --access public
        working-directory: packages/virage-store-lancedb
        env:
          NODE_AUTH_TOKEN: ${{ secrets.NPM_TOKEN }}

  publish-store-chromadb:
    name: Publish @vivantel/virage-store-chromadb
    needs: release-please
    if: ${{ needs.release-please.outputs.chromadb_released == 'true' }}
    runs-on: ubuntu-latest
    permissions:
      contents: read
      id-token: write
    steps:
      - uses: actions/checkout@v6
      - uses: actions/setup-node@v6
        with:
          node-version: '20.x'
          registry-url: 'https://registry.npmjs.org'
          cache: "npm"
      - run: npm ci --ignore-scripts
      - run: npm run build --workspace packages/virage-core
      - run: npm test --workspace packages/virage-store-chromadb
      - run: npm run build --workspace packages/virage-store-chromadb
      - name: Publish
        run: npm publish --provenance --access public
        working-directory: packages/virage-store-chromadb
        env:
          NODE_AUTH_TOKEN: ${{ secrets.NPM_TOKEN }}

  publish-cli:
    name: Publish @vivantel/virage-cli
    needs: release-please
    if: ${{ needs.release-please.outputs.cli_released == 'true' }}
    runs-on: ubuntu-latest
    permissions:
      contents: read
      id-token: write
    steps:
      - uses: actions/checkout@v6
      - uses: actions/setup-node@v6
        with:
          node-version: '20.x'
          registry-url: 'https://registry.npmjs.org'
          cache: "npm"
      - run: npm ci
      - run: npm run build --workspace packages/virage-core
      - run: npm run build --workspace packages/virage-dashboard
      - run: npm test --workspace packages/virage-cli
      - run: npm run build --workspace packages/virage-cli
      - name: Publish
        run: npm publish --provenance --access public
        working-directory: packages/virage-cli
        env:
          NODE_AUTH_TOKEN: ${{ secrets.NPM_TOKEN }}

  publish-dashboard:
    name: Publish @vivantel/virage-dashboard
    needs: release-please
    if: ${{ needs.release-please.outputs.dashboard_released == 'true' }}
    runs-on: ubuntu-latest
    permissions:
      contents: read
      id-token: write
    steps:
      - uses: actions/checkout@v6
      - uses: actions/setup-node@v6
        with:
          node-version: '20.x'
          registry-url: 'https://registry.npmjs.org'
          cache: "npm"
      - run: npm ci
      - run: npm run build --workspace packages/virage-dashboard
      - name: Publish
        run: npm publish --provenance --access public
        working-directory: packages/virage-dashboard
        env:
          NODE_AUTH_TOKEN: ${{ secrets.NPM_TOKEN }}

  publish-github-packages:
    name: Publish @vivantel/virage-core to GitHub Packages
    needs: release-please
    if: ${{ needs.release-please.outputs.core_released == 'true' }}
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write
    steps:
      - uses: actions/checkout@v6
      - uses: actions/setup-node@v6
        with:
          node-version: '20.x'
          registry-url: 'https://npm.pkg.github.com'
          cache: "npm"
      - run: npm ci
      - run: npm run build --workspace packages/virage-core
      - name: Publish to GitHub Packages
        run: npm publish
        working-directory: packages/virage-core
        env:
          NODE_AUTH_TOKEN: ${{ secrets.GITHUB_TOKEN }}

  deploy-docs:
    name: Deploy API Docs
    needs: release-please
    if: ${{ needs.release-please.outputs.core_released == 'true' }}
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v6
      - uses: actions/setup-node@v6
        with:
          node-version: '20.x'
          cache: "npm"
      - run: npm ci
      - run: npm run build --workspace packages/virage-core
      - name: Generate API documentation
        run: npm run docs:generate --workspace packages/virage-core
      - name: Deploy to GitHub Pages
        uses: peaceiris/actions-gh-pages@v4
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          publish_dir: ./docs/api
          keep_files: false
