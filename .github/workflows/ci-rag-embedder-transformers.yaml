name: CI — @vivantel/rag-embedder-transformers

on:
  push:
    branches: [master, develop]
    paths:
      - 'packages/rag-embedder-transformers/**'
      - '.github/workflows/ci-rag-embedder-transformers.yaml'
  pull_request:
    branches: [master]
    paths:
      - 'packages/rag-embedder-transformers/**'
      - '.github/workflows/ci-rag-embedder-transformers.yaml'

jobs:
  build-and-test:
    name: Build & Test
    runs-on: ubuntu-latest

    strategy:
      matrix:
        node-version: [22, 24, 26]
      fail-fast: false

    steps:
      - uses: actions/checkout@v6

      - uses: actions/setup-node@v6
        with:
          node-version: ${{ matrix.node-version }}
          cache: "npm"

      - name: Install dependencies
        run: npm ci --ignore-scripts

      - name: Build rag-core
        run: npm run build --workspace packages/rag-core

      - name: Type check
        run: npm run type-check --workspace packages/rag-embedder-transformers

      - name: Run tests
        run: npm test --workspace packages/rag-embedder-transformers

      - name: Build
        run: npm run build --workspace packages/rag-embedder-transformers
