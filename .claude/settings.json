{
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Bash",
        "hooks": [
          {
            "type": "command",
            "if": "Bash(*git commit*)",
            "command": "npm run fix && npm run type-check:ci && git add -u",
            "statusMessage": "Running fix + type-check before commit..."
          }
        ]
      }
    ]
  }
}
